describe('useProcessEvent', () => {
  describe('before unmounting the component', () => {
    xit('should call the callback with all of the original event arguments.', () => {});
    describe('after unmounting the component', () => {
      xit('should skip calling the callback.', () => {});
    });
  });
});
