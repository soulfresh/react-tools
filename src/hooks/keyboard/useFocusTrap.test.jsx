
describe('useFocusTrap', () => {
  describe('when active', () => {
    describe('after pressing Tab', () => {
      xit('should focus the first element in the trap.', () => {});
      describe('and then pressing Shift + Tab', () => {
        xit('should focus the last element in the trap.', () => {});
      });
      describe('through all elements in the trap', () => {
        xit('should return focus to the first element in the trap.', () => {});
      });
    });
  });
  describe('when not active', () => {
    describe('when tabbing forwards through the trap', () => {
      xit('should allow tab focus to leave the trap.', () => {});
    });
    describe('when tabbing backwards through the trap', () => {
      xit('should allow tab focus to leave the trap.', () => {});
    });
  });
});
