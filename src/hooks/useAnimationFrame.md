<a name="useAnimationFrame"></a>

## useAnimationFrame()
Returns a wrapper around `requestAnimationFrame` that will
automatically clear the request if the component
is destroyed before the frame is rendered.

**Kind**: global function  
