
describe('useAnimationFrame', () => {
  describe('before the next frame', () => {
    xit('should not call the callback.', () => {});
    xit('should return the id of the animation frame.', () => {});

    describe('and being destroyed', () => {
      xit('should not call the callback on the next frame.', () => {});
    });
  });

  describe('in the next animation frame', () => {
    xit('should call the callback.', () => {});
  });
});
