
describe('Action', () => {
  xit('should be able to render a button.', () => {});
  xit('should be able to render a routed link.', () => {});
  xit('should be able to render an unrouted link.', () => {});
  xit('should be able to render an external link.', () => {});
  describe('analytics', () => {
    xit('should be able to perform event tracking on a button.', () => {});
    xit('should be able to perform event tracking on a link.', () => {});
    xit('should be able to perform external link tracking on a link.', () => {});
    xit('should never perform external link tracking on a button.', () => {});
    xit('should not perform event tracking without a category.', () => {});
    xit('should not perform event tracking without a label.', () => {});
    xit('should not perform event tracking without an action.', () => {});
  });
});
